export type EventItem = {
  id: string;
  title: string;
  date: string; // ISO date (YYYY-MM-DD)
  time: string; // Time range (HH:MM-HH:MM)
  place: string;
  host: string;
  address: string;
  lat: number;
  lon: number;
  about: string;
  bring: string;
  culture: string;
};
